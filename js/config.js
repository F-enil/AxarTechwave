const CONFIG = {
    // REPLACE THIS WITH YOUR RENDER URL (e.g., https://your-app.onrender.com/api)
    API_URL: 'https://axartechwave.onrender.com/api',
    // Cloudinary returns full URLs, so this is likely unused now, but keeping for safety
    MEDIA_URL: 'http://localhost:9000/ecommerce-media',
};
// force update 
